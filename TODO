Look into the following errors encountered when the database is seeded.
Meta PaymentStatus is not supported for resource *models.Subscription, no `Valuer` configured for it
Duplicated meta McCurrency defined for resource Transactions
